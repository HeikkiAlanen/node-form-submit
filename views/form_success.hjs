{{<layout}}
	{{$content}}
		{{#messages}}
			<div class="pure-form-messages">
				<p class="pure-form-message pure-form-message-{{type}}">{{message}}</p>
			</div>
		{{/messages}}

		<img src="/assets/gandalf.gif" alt="" />

	{{/content}}
{{/layout}}
